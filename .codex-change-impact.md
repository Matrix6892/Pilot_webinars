# Change impact: свободный находчивый агент, Flash experiment и adversarial live-gate

## Ledger T2-2: «мысль вслух» в фокус-панели заказа 2026-08-23

### Наблюдаемое поведение

- До: после отправки заявки пользователь видел стадии («проверяю склад…»)
  без конкретики; пожелание USER #4/#7 — ожидание должно показывать, НАД ЧЕМ
  агент думает.
- После: детали существующих событий `vision` и `primary` обогащены живым
  контекстом моста — «Смотрю на снимок „fence.jpg“ из заявки „…“»,
  «Разбирая запрос „…“: сверяю каталог, свежий остаток на складе…», признак
  продолжения переписки. Фокус-панель «Текущий фокус · в работе» показывает
  деталь последнего подтверждённого события крупно (elapsed и пульс уже
  были). Новых stage нет; форма события (stage/title/detail/state) и
  fast/live-контракт не менялись.

### Инварианты, tests и rollback

- Regression: `tests/vision-bridge.test.mjs` — «stage events carry a live
  thinking line into the focus panel and CSV» (addEvent-spy + regex моста/UI);
  падает без изменения. Полный gate 628/628 + lint/typecheck/build +
  оба контракта PASS.
- Reasoning/секреты не сохраняются — только контекст, известный мосту до
  запуска модели. CSV formula-safe не нарушен.
- Rollback: revert трёх файлов (agent-bridge.mjs, order-stand.tsx,
  vision-bridge.test.mjs), данные совместимы.

## Ledger T1-10: legacy-цены выведены из активных доков 2026-08-23

### Наблюдаемое поведение

- До: учебные цифры старого складского эпизода (620/420/254 700 ₽) оставались
  в трёх активных доках с пометкой «secondary/legacy», а контракт-аудит
  наоборот ТРЕБОВАЛ их присутствия (устаревшее правило secondaryScenarioChecks).
- После: README.md, docs/WEBINAR-RUNBOOK.md, docs/ARCHITECTURE-AND-WEBINAR.md
  содержат только 2-3-строчные указатели на
  `docs/archive/ARCHITECTURE-AND-WEBINAR-v1.md`; полный исторический контекст —
  в архиве. Аудит инвертирован: активные доки обязаны НЕ содержать
  легаси-цифр (secondaryLegacyAbsentFrom), архив обязан хранить историю.
  Каноника КР-001 не тронута (2000/300/1700 кг, ПромКолор Опт, 718 400 ₽).

### Инварианты, tests и rollback

- audit-contract → PASS; check-change-contract 21/21 PASS; lint починен в
  app/economics/page.tsx (`<a>` → `<Link>`, правило no-html-link-for-pages);
  полный локальный gate на этой ветке: typecheck/build/tests 627/627 PASS.
- Rollback: revert правки аудита вернёт требование присутствия легаси-блоков
  (противоречит плану L4) — не рекомендуется.

## Live holdout: стабилизация wow-03 после rapport-правок 2026-08-05

### Наблюдаемое поведение

- До (после первой rapport-вставки): holdout wow-03-hostile-grill 0/3 по
  `heat_food_boundary` — клиентский текст перестал называть причину границы
  (огонь/пища/внутренняя зона), уходил в «совместимость не подтверждена»;
  в одном прогоне — yellow/estimate по наружной части мангала вместо
  manager/none; в другом — зеркалирование «гений продаж».
- После: 3/3 hard invariants, качество 100%, red/manager/none во всех
  прогонах. Правки: причина границы называется словами клиента (огонь,
  контакт с пищей, внутренняя зона или поверхность, человек, животное);
  достижимая версия просьбы при огне/пище/человеке/животном = руководитель
  без чисел; подыгрывание — только дружелюбному юмору, колкости не зеркалятся.
- Остаточный наблюдённый флак вне wow-03: wow-08 повтор 3/12 — 0 supplier
  leads (недетерминированность живого веб-поиска; 2/3 прогонов с лидом).

### Инварианты, tests и rollback

- Полный gate 627/627; raw runs: /tmp/koler-wow-live{,-fix,-fix2,-fix3,-fix4}.
- Rollback: revert 3943cd9 возвращает rapport-вставки без стабилизаторов
  (не рекомендуется: возвращает 0/3 по wow-03).

## Вебинарный тюнинг: rapport, анти-fail-closed вставки и taste-skill UI 2026-08-05

### Наблюдаемое поведение

- До: шутка клиента («покрасьте кота», «торт кремом») получала холодно-нейтральный
  тон; животные не были защищены границей каталога (формально проходили как
  необычный неодушевлённый объект с estimate); reviewer мог браковать игривый
  тон как давление; blocker choices и supplierLead.url не имели явных правил
  дословности → периодические fail-closed и coverage-repair.
- После: sales-agent 6.5 — тёплая конвертация шутки в реальный шаг (вне
  единственного blocker-ответа), животные = `product=null` + тёплая граница +
  мост к настоящему объекту; reviewer 6.6 — игра с шуткой = Rapport, зеркало
  животных. Добавлены правила: `choices` — дословные копии label;
  photo-visible ask подтверждается цитатой письма; `url` — без ручных правок;
  reply.body ≤1200 знаков. webfetch default timeout 30→20 c (хвост −50 c worst
  case). Docs cap синхронизирован с opencode.json (32 768).
- UI (ветка ui-refresh): пол кегля проектора (10/11→11/12px), easing-кривая,
  токены спеки docs/UI-REFRESH-SPEC.md (акцент #8a4a16, AA-варианты маршрутных
  цветов, CTA постоянной охрой, деунификация рядов, нейтральные window-dots,
  сетка live-inventory, офлайн-точка muted вместо жёлтого).
- Не меняется: AgentResult v2, маршруты/зоны (#23704c/#bd791b/#b94034),
  guard-контракты, число model calls, D1 schema, capability auth.

### Инварианты, tests и rollback

- Reviewer prompt удержал gate <20 000 символов за счёт стилистических сжатий
  без изменения норм (запас 98).
- Rollback слоями: revert 8c120b3 / 5740445 / cb7440b / c6e0d54 либо
  `git checkout main`; UI-слой самодостаточен в конце globals.css.
- Проверка локально: lint + typecheck + node --test tests/*.test.mjs (627/627)
  + audit-contract (no findings) + check-change-contract (0 FAIL) +
  eval-readiness (26/26) + deployed read-only preflight (GO WITH FALLBACK).
- Перед эфиром обязателен платный holdout wow-02/03/08/09 ×3 (промпты менялись);
  vision cap 180 c оставлен: фактических таймингов в локальной D1 нет.

## Manager approve не публикует пропущенный ask 2026-08-05

### Наблюдаемое поведение

- До: deployed follow-up «как подготовить деревянный забор перед покраской»
  переживал reload и доходил до reviewer, но эвристика распознавала подготовку
  только рядом со словами «поверхность/основание». Reviewer честно блокировал
  ответ, а manager approve затем всё равно публиковал прежнюю оценку без ответа
  на новый вопрос.
- После: общая preparation-эвристика также понимает форму «подготовить объект
  перед покраской»; нормализатор добавляет безопасные конкретные действия.
  Любой non-clarification approve дополнительно проходит `askCoverageGaps` до
  history/event/update и получает `409 + recalculate`, если выбранное письмо
  всё ещё пропускает явную просьбу.
- Не меняется: AgentResult v2, число model calls, D1 shape, manager options,
  capability auth, CAS-транзакция и fail-closed reviewer.

### Канонический владелец и workflows

- Источник полноты: `lib/agent-guard.mjs`; последний publish guard:
  `app/api/orders/route.ts`; regressions: `tests/agent-guard.test.mjs` и
  `tests/flow-integrity.test.mjs`.
- [x] Agent contract
- [x] Order lifecycle
- Остальные workflows не меняются; persistence/UI/ledger читают прежний result.

### Инварианты, tests и rollback

- Неполный model reply не становится клиентским завершением ни напрямую, ни
  после manager approve; target-ambiguity по-прежнему может отправить один
  вопрос клиенту.
- Regression: exact follow-up нормализуется в конкретную подготовку; approval
  coverage guard стоит до history/event/update. Targeted agent/flow suites,
  затем полный gate и повтор deployed reload → follow-up → approve.
- Rollback: вернуть preparation-pattern и approval guard вместе, если валидный
  manager reply ошибочно блокируется; schema/data rollback не нужен.

### Итог локальной проверки

- Regression/targeted: 302/302.
- Full gate: lint PASS, typecheck PASS, `node:test` 627/627, build PASS,
  change-contract 20/20, `git diff --check` PASS.
- Не проверено после последнего изменения: опубликованный follow-up,
  reload и manager smoke.
- Deploy выполнен: нет.

### Recovery после честного coverage-409

- Deployed gate версии 52 доказал новый край: approval правильно вернул 409
  для неполного варианта, но действие `recalculate` затем отвергало свежий
  заказ, потому что знало только stale inventory/supplier и общий `error`.
- После: существующий recalculate-path разрешён также тогда, когда сохранённый
  result программно имеет `askCoverageGaps`; тот же order получает ровно один
  новый round. Deployed runner следует `409 + recalculate` один раз и всё равно
  требует полезный non-error terminal в пределах общего лимита.
- Не меняется: state schema, endpoint, число repair-вызовов внутри одного
  model-chain, capability/CAS/history и fail-closed publish guard.
- Regression: wiring recalculate проверяет coverage до fresh-result rejection;
  runner ограничивает recovery одним повтором. Затем полный local/deployed gate.
- Локально: targeted 196/196, full `node:test` 627/627, lint, typecheck,
  build, change-contract 20/20, contract audit и main scenario PASS.
- Deploy recovery-изменения выполнен: нет.

## OpenCode cold-start latency 2026-08-05

### Наблюдаемое поведение

- До: чистый deployed batch из 10 заявок получил первую processing-stage за
  1–4 секунды и 10/10 полезных terminal results, но p50 составил 206,823 с.
  OpenCode logs показывают по 20–30 секунд до provider stream на каждом primary
  и reviewer. Отключение внешних skills не убрало обязательные snapshot и file
  watcher; следующий batch снова вышел за p50 и одна заявка дошла до
  `primary-fallback` после coverage-repair.
- После: одноразовый read-only OpenCode сохраняет project agents и
  `public_webfetch`, но получает документированные `snapshot:false` и
  `OPENCODE_EXPERIMENTAL_DISABLE_FILEWATCHER=true`. Изолированный benchmark
  дошёл до provider stream за 1,409 с вместо 20–30 с. DeepSeek Flash/max,
  prompts, output budget и все fail-closed проверки остаются прежними.

### Канонический владелец, инварианты и проверка

- Владелец: `scripts/agent-bridge.mjs`; regression:
  `tests/vision-bridge.test.mjs`; workflow: Bridge lease.
- Инварианты: capability/secrets allowlist не расширяется; custom project tool
  остаётся доступен sales-agent; vision, primary и reviewer получают отдельную
  SQLite; никакой model/result/API/D1/UI контракт не меняется.
- Проверка: executable child-environment regression, targeted bridge suite,
  полный local gate, затем новый deployed batch-10 с прежними порогами
  first-stage ≤10 с, p50 ≤180 с и 10/10 ≤660 с.
- Rollback: удалить две runtime-настройки и regression, если OpenCode перестанет
  загружать project agent/plugin или read-only bridge неожиданно начнёт менять
  файлы.

### Финальное live-доказательство

- Канонический launchd bridge работает с `ProcessType=Interactive` и
  `BRIDGE_CONCURRENCY=10`; это устранило искусственное QoS-ограничение фонового
  процесса без изменения очереди или model-chain.
- Чистый deployed batch из 10 заявок: 10/10 полезных terminal results,
  first-stage max 6 796 мс, p50 136 305 мс, max 237 355 мс. Конкурентный
  follow-up дал ровно один `200` и один `409`, история сохранена.
- Девять функциональных deployed probes прошли 9/9, включая точный запрос
  «забор + Кремль», фото, manager approve, reload/capability и следующий round.

## Coverage repair preserves every ask 2026-08-05

### Наблюдаемое поведение

- До: единственный coverage-repair видел только gaps первого draft. Исправляя
  их, live-модель могла потерять уже закрытый ask; финальный guard честно
  создавал `primary-fallback` под параллельной нагрузкой.
- После: тот же единственный repair получает полный bounded список просьб —
  валидные asks первого draft плюс gaps — и обязан сохранить каждый пункт в
  новом полном draft. Если безопасный authored lead теряет контекст цветового
  ответа для дополнительного объекта, открытое web-evidence о памятнике или
  наследии позволяет canonical reply добавить общий совет сохранить
  утверждённый исторический облик.
- Дополнительный deployed probe «металлические ворота + бюджет» выявил не
  пропуск модели, а конфликт двух guard-инвариантов: санитаризация
  неподтверждённой атрибуции заменяла каждый estimate `method` безымянной
  строкой, после чего coverage не мог связать готовый budget с объектом. Теперь
  санитаризация сохраняет только короткий объектный префикс, чьи смысловые
  корни уже есть в последнем сообщении клиента; неподтверждённые SKU, цены и
  названия по-прежнему удаляются.
- Не меняется: число model calls, AgentResult v2, guard, reviewer, API/D1/UI,
  deadlines и fail-closed fallback.

### Канонический владелец

- Источники истины: `scripts/agent-bridge.mjs`, `lib/agent-guard.mjs`;
  regressions: `tests/vision-bridge.test.mjs`, `tests/agent-guard.test.mjs`.
- Читатели: prompt единственного coverage-repair и canonical estimate reply;
  persistence и presentation не меняются.
- Workflows: Agent contract, Bridge lease.

### Инварианты, tests и rollback

- Список ограничен шестью asks; gaps имеют приоритет и не подменяются
  модельной памятью.
- Regression: repair prompt содержит и ранее закрытый ask, и текущий gap;
  protected color advice появляется только при релевантном открытом
  web-evidence; затем targeted/full gate и deployed runner.
- Rollback: вернуть repair-вызову только `coverageGaps` и удалить canonical
  protected advice вместе с regressions.
- Deploy выполнен: нет.

## Ordinary follow-up continuation 2026-08-05

### Наблюдаемое поведение

- До: bridge передавал структурированный результат прошлого раунда только после
  `target_ambiguity`; обычный follow-up после `ready_to_send` видел переписку,
  но терял прежние asks, target и evidence и дважды завершался
  `primary-fallback`.
- После: любой новый round с валидным сохранённым AgentResult v2 получает тот же
  bounded continuation context; просьба о подготовке поверхности закрывается
  конкретными действиями подготовки без буквального повторения вопроса; свежий
  ответ клиента важнее, а коммерческие числа по-прежнему строятся только из
  текущих snapshots.
- Не меняется: AgentResult v2, asks, один repair, prompts, API/D1/UI, reviewer,
  fail-closed path и runtime limits.

### Канонический владелец

- Источники истины: `scripts/agent-bridge.mjs`, `lib/agent-guard.mjs`;
  regression: `tests/vision-bridge.test.mjs`, `tests/agent-guard.test.mjs`.
- Читатели: live primary prompt и coverage gate; persistence/presentation не
  меняются.
- Workflows: Agent contract, Bridge lease.

### Инварианты, tests и rollback

- Контекст используется только при строго большем round и валидном v2;
  inventory/supplier evidence остаётся историческим и не подтверждает новое
  обязательство.
- Regression: resolved continuation после ambiguity и после обычного ready;
  конкретная подготовка проходит, generic acknowledgement не проходит;
  targeted `agent-guard`/`vision-bridge`, затем полный gate и deployed probes.
- Rollback: вернуть blocker-condition, preparation facet и regressions вместе.
- Deploy выполнен: нет.

## Final coverage и единый webinar bridge 2026-08-04

### Наблюдаемое поведение

- До: старый launchd bridge и новый ручной bridge одновременно забирали live-
  заявки. Поэтому карточка могла пройти старую нормализацию после зелёного
  локального gate. Кроме того, цветовой совет внутри секций «По забору» и «По
  Кремлю» терял объект при разбиении ответа на отдельные предложения, а после
  reviewer не было последней проверки полноты перед публикацией.
- После: на машине работает только канонический launchd bridge; цветовой совет
  наследует объект своей секции, включая рекомендацию вида «правильный вариант —
  сохранить цвет»; для защищённого объекта primary и единственный repair явно
  рекомендуют сохранить утверждённый исторический облик, а не останавливаются
  на фразе «цвет определят специалисты»; одна рекомендация после общего
  упоминания двух объектов больше не закрывает оба; любой `route=ready` повторно проходит
  `askCoverageGaps` после reviewer и не публикуется при пропущенной просьбе.
  Deployed probe принимает явный совет сохранить палитру либо канонический
  красно-кирпичный вид как содержательно эквивалентные ответы.
- Не меняется: AgentResult v2, D1/API shape, один coverage-repair, Flash/max,
  polling/events, 650/660-секундные лимиты и fail-closed error path.

### Канонический владелец

- Источник истины полноты: `lib/agent-guard.mjs`; место последней публикации:
  `scripts/agent-bridge.mjs`; deployed acceptance: `tests/run-webinar-probes.mjs`.
- Читатели: live bridge, recorded guard tests и deployed probe runner.
- Писатели: primary и reviewer не получают новых полей; persistence, UI и
  ledger не меняются.

### Затронутые workflows

- [x] Agent contract
- [ ] Order lifecycle
- [ ] API boundary
- [x] Bridge lease
- [ ] Schema evolution
- [ ] Catalog/scenario
- [ ] Observable event

### Инварианты и риски

| Инвариант | Доказательство до | Проверка после |
|---|---|---|
| Один актуальный runtime обслуживает очередь | обнаружены два PID с одним cwd | ровно один launchd PID после restart |
| Секция сохраняет объект цветового совета | replay assistant text последнего live-run | exact sectioned regression |
| Пропущенный ask не становится ready | persisted №3980 имел coverage gap | post-review publication guard |
| Manager/ambiguity path не ломается | guard применяется только к `route=ready` | bridge + full suites |

### Tests

- Regression: секционные цветовые советы для двух объектов, фактическая live-
  формулировка «правильный вариант — сохранить», protected-object postcondition
  в primary/repair, запрет общего цветового совета на два объекта и post-review
  ready guard.
- Targeted: `agent-guard`, `vision-bridge`, `flow-integrity`.
- Full gate: lint, typecheck, все node tests, build, change-contract, eval и
  contract audit.
- Live: свежий №3980, девять probes, concurrent-10 и три browser smoke.

### Rollback

- Условие: секционный контекст ошибочно закрывает цвет другого объекта либо
  корректный ready-result блокируется после reviewer.
- Действие: откатить paragraph candidate и финальную ready-проверку вместе с
  regression; runtime снова запускать только через launchd.
- Совместимость данных: schema и сохранённые карточки не меняются.

### Итог

- Реализовано: section-aware color coverage, post-review ready guard и
  семантически точный deployed acceptance.
- Проверено: replay raw assistant text последнего live-run, targeted 255/255,
  полный набор 623/623, lint, typecheck, build, change-contract 20/20,
  eval-readiness 26/26, contract audit и local main scenario.
- Не проверено: свежий deployed №3980, девять deployed probes,
  concurrent-10 и browser smoke.
- Deploy выполнен: нет.

## Наблюдаемое поведение

- До: два контрольных live-прогона показали, что primary тратит 165–360 секунд
  на обязательную генерацию производных legacy-полей; timeout reviewer стирает
  уже безопасный ответ; нейтральный след открытия URL может выглядеть как
  содержательное доказательство; итоговый scorer не отделяет инициативу модели
  от работы guard и оценивает пять качеств вместо шести.
- После этого этапа, до переключения direct API: primary возвращал компактное
  смысловое ядро, а производные поля
  достраивает единый guard; fail-closed reviewer сохраняет безопасную помощь и
  переводит обязательство руководителю; audit URL не закрывает claim-specific
  факт; live-gate отдельно показывает raw-primary и final-system по шести
  качествам с N/A и проверял перенос правил metamorphic-подстановками. На этом
  историческом этапе default primary оставался
  `opencode-go/deepseek-v4-pro` с variant `max`, а
  независимый reviewer — отдельным запуском той же Pro/max.
  `opencode-go/deepseek-v4-flash` доступен как экспериментальный alias без
  заявления конкретной версии провайдера. Актуальное переключение на direct
  Flash зафиксировано в разделе от 2026-08-01 ниже.
- Не меняется: `AgentResult` schemaVersion 2, snapshot ground truth,
  арифметика дефицита, privacy-граница vision, lease/fencing и запрет точного
  обязательства по публичному supplier lead.

## Канонический владелец

- Источник истины: `lib/demo-engine.ts` для `AgentResult`,
  `lib/agent-guard.mjs` для normalization, `public/prompts/*.md` для ролей и
  поведения, `scripts/agent-bridge.mjs` для live-chain.
- Читатели: bridge, agent API, orders API, UI, history, ledger и eval runner.
- Писатели: primary/reviewer модели; программа принимает только нормализованный
  результат и observed completed tool state.
- Persistence: persisted schema не меняется; `openedSources` — эфемерный
  runtime input guard, итоговый web audit хранится обычным `OrderEvidence`.
- UI/ledger/docs: новый `review-skipped` имеет русское представление в карточке
  и CSV; единый model catalog задаёт default и variant для UI/API/bridge;
  README, architecture и behavior standard описывают фактический путь.

## Затронутые workflows

- [x] Agent contract
- [ ] Order lifecycle
- [ ] API boundary
- [x] Bridge lease
- [ ] Schema evolution
- [x] Catalog/scenario
- [x] Observable event

## Инварианты и риски

| Инвариант | Доказательство до | Проверка после |
|---|---|---|
| Нет сценарных веток по предмету/фильму | LLM-first primary | anti-copy holdout и source scan |
| Свободный смысл не зависит от языка и риторики | message/vision evidence | mixed-language, brand и correction regressions |
| URL не доказывает содержание страницы | completed direct tool state | neutral-evidence test, failed/search exclusion |
| Safety boundary не выдумывает вред | canonical applicability fallback | risk-rhetoric removal и safe-action regression |
| Fail-closed primary не создаёт лишний долгий reviewer-run | complete manager fallback | bridge wiring и runner policy tests |
| Исследование завершимо на вебинаре | tool events и elapsed time | one-search/direct budget и 700 s latency manifest |
| Guard не получает баллы за primary | raw и normalized artifacts | раздельный raw/system score по шести качествам |
| Правило переносится на новый объект | frozen holdout | metamorphic-подстановки без словаря предметов |
| Reviewer fail-closed не уничтожает помощь | safe primaryGuarded | manager/red/none с сохранённым связным reply |
| Новое событие понятно во всех projections | generic event persistence | ledger CSV и rendered UI tests |
| Новый Flash не смешивается с reviewer | `data/models.json` + fixed reviewer | primary Flash/max, reviewer Pro/max в runtime и live artifact |
| URL поставщика не ломается из-за `/` | canonical URL helper | regression с trailing slash |

## Tests

- Regression: роли Flash/max и Pro/max, trailing-slash supplier URL, реальные
  raw-сбои Porsche/mixed language, safety rhetoric,
  neutral opened evidence, supplier lead, reviewer timeout, compact draft,
  raw/system attribution, metamorphic transfer, efficiency и latency.
- Targeted: `agent-guard-wow`, `agent-contract-v2`, `run-agent-wow-live`,
  `vision-bridge`, `model-role-honesty`, `ledger-export`, `rendered-html`.
- Full gate: lint, typecheck, полный `node --test`, build, change-contract,
  canonical main scenario и `git diff --check`.
- Fault/concurrency: существующие fake-clock lease, stale claim, serial polling,
  reconciliation и cleanup failure остаются в полном наборе.

## Rollback

- Условие: нейтральный web audit начинает подтверждать содержательные факты,
  safe fragment пропускает обещание/SKU или latency policy скрывает валидный
  результат.
- Действие: удалить только synthetic opened evidence, boundary-action lead или
  отдельный eval gate; контракт v2, snapshots и fencing не откатывать.
- Совместимость данных: migration и persisted shape не менялись.

## Итог

- Реализовано: общий service charter, 10-case frozen holdout, 80 paraphrases,
  доказательная safety boundary, bounded web research, latency manifest и
  fail-closed reviewer policy.
- Проверено: targeted contract tests, сохранённый Pro replay, полный локальный
  gate (lint, typecheck, 314/314 tests, build, change-contract), contract audit
  без findings и четырёхкейсный Pro/max live-прогон с лимитом 700 секунд.
- Live-ограничение: повтор `wow-03/04/06/07` не прошёл gate — hard invariants
  провалены в `wow-03/04`, итоговый JTBD 58,33%. Один primary завершился за
  445 секунд; остальные три не вернули JSON за 600 секунд и 90-секундный
  synthesis. Увеличение timeout предотвращает преждевременный обрыв, но не
  является доказанным исправлением нестабильности провайдера или качества.
- Не проверено: production D1/R2, production migration, deploy, реальная
  ERP/email интеграция и три успешных live-повтора четырёх webinar-сценариев.
- Deploy выполнен: нет.

## Webinar completion contract 2026-08-04

### Наблюдаемое поведение

- До: составной live-запрос мог потерять просьбу; manager approve завершал
  внутренний workflow без явного клиентского завершения; reload терял JS key;
  один bridge worker не обеспечивал пачку вебинара.
- После: `ResolvedIntent.asks` и generic coverage/reviewer gate закрывают все
  просьбы; `ready_to_send` раскрывает полный ответ и 100%; HttpOnly capability
  переживает reload; follow-up атомарно создаёт следующий round; UI показывает
  только persisted stages; bridge запускает 1–10 fenced workers с deadline
  650 секунд, deployed gate ограничен 660 секундами от POST.
- Не меняется: AgentResult остаётся v2, D1 schema и endpoints не добавляются,
  send/reserve остаются событиями стенда, reasoning не сохраняется и не
  показывается.

### Владельцы и проверки

- Contract/guard: `lib/demo-engine.ts`, `lib/agent-guard.mjs`, prompts.
- Lifecycle/API: `app/api/orders/route.ts`, `app/api/agent/route.ts`.
- UI/events: `app/order-stand.tsx`, `lib/client-request.mjs`.
- Bridge/gate: `scripts/agent-bridge.mjs`, `tests/run-webinar-probes.mjs`.
- Regression: asks/legacy, exact забор+Кремль, reviewer blocking, cookie/reload,
  follow-up matrix/concurrency, life stages, worker pool/deadlines/Flash identity.
- Локально: lint, typecheck, build, 618/618 tests, eval 26/26,
  change-contract и contract audit PASS.

### Rollback

- Условие: legacy v2 перестаёт читаться, один follow-up создаёт два эффекта,
  claim fencing нарушается либо deployed gate не получает 10/10.
- Действие: откатить эту wave целиком; schema/data/history не мигрировать и не
  редактировать историческую №3980.
- Deploy выполнен: нет.

## Flash reviewer + compound customer reply 2026-08-04

### Наблюдаемое поведение

- До: primary использует `deepseek/deepseek-v4-flash`, но отдельный reviewer
  запускается на `opencode-go/deepseek-v4-pro`. Live-заявка №6830 получила
  `yellow/needs_info`, а клиентское письмо закрыло только забор: ответ про
  Кремль и рекомендация цвета исчезли, хотя reviewer одобрил результат.
- После: отдельный reviewer запускается вторым прямым Flash-вызовом без
  инструментов. Guard/reviewer отклоняет составной результат, если
  `customerResponse` не отвечает на каждую распознанную часть запроса;
  безопасные этапы UI везде называют Flash.
- Не меняется: reviewer остаётся отдельным fail-closed вызовом, получает тот же
  immutable packet, не использует web/tools и не раскрывает hidden reasoning.

### Канонический владелец и consumers

- Источник истины модели: `scripts/agent-bridge.mjs`; каталог ролей:
  `data/models.json`; provenance/default: `lib/agent-guard.mjs`.
- Контракт полноты: sales/reviewer prompts и `lib/agent-guard.mjs`.
- Читатели: bridge events, order UI, ledger, model-role/bridge/guard tests и
  операторские contract checks.
- Persistence/API shape не меняются; старые карточки с Pro provenance остаются
  читаемыми.
- Workflows: Agent contract, Bridge lease, Observable event.

### Инварианты и риски

| Инвариант | Доказательство до | Проверка после |
|---|---|---|
| Reviewer отдельный и без инструментов | №6830 содержит отдельные review events | transport test: второй Flash call, immutable packet, no tools |
| Reviewer failure остаётся fail-closed | recorded malformed reviewer retry | targeted bridge/guard tests |
| Все части compound query видны клиенту | №6830 теряет Кремль/цвет | exact regression с текстом вебинара |
| Старый provenance читается | сохранённые №3980/6830 используют Pro | ledger/guard backward-compat tests |

### Tests и rollback

- Regression: точный запрос про деревянный забор и Кремль; письмо содержит
  КР-005, расход/слои/цену/цвета и отдельную границу/оценку для Кремля.
- Targeted: `agent-guard`, `model-role-honesty`, `vision-bridge`,
  `rendered-html`, `ledger-export`.
- Full gate: lint, typecheck, все tests, build, change contract.
- Rollback: вернуть только reviewer routing/labels и compound completeness
  delta; schema/order history не меняются.
- Deploy выполнен: нет.

## Webinar closeout: cultural discovery budget 2026-08-03

### Наблюдаемое поведение

- До: семантически корректный cultural run с одной открытой прямой страницей
  падал только потому, что Flash выполнил два discovery-запроса вместо одного.
- После: культурный референс допускает до двух discovery-запросов; обычный
  внешний вопрос по-прежнему допускает один. Доказательством остаётся максимум
  одна содержательно релевантная открытая страница.
- Не меняется: runtime, guard, AgentResult V2, direct-attempt budget, запрет
  search snippets как evidence, роли моделей и общий deadline 700 секунд.

### Канонический владелец и consumers

- Источник истины: `tests/run-agent-wow-live.mjs`, функция
  `evaluateResearchEfficiency`.
- Consumers: live artifact, manifest и webinar certification gate.
- Prompts/profiles только объясняют тот же bounded budget; persistence и UI не
  меняются.

### Инварианты и проверки

| Инвариант | Regression |
|---|---|
| Cultural: 2 discovery + 1 valid direct page проходят | replay формы последнего wow-09 artifact |
| Ordinary: 2 discovery по-прежнему не проходят | synthetic ordinary mutant |
| Cultural: 3 discovery не проходят | upper-bound mutant |
| Search page не становится evidence | существующий cultural oracle suite |

### Rollback

- Условие: два поиска разрешаются обычным задачам или больше одной cultural
  страницы влияет на решение.
- Действие: откатить только per-contract search limit и его regression.
- Deploy выполнен: нет.

## Дожим после Luna-аудита 2026-08-01

### Наблюдаемое поведение

- До: complete partial JSON может потеряться из-за telemetry error; полный
  fail-closed `manager/red/none` иногда зря запускает reviewer; generic fallback
  получает JTBD-баллы за работу guard; поздняя коррекция количества может
  проиграть старому значению; публичные order/ledger/upload reads раскрывают
  больше данных, чем требуется стенду; UI не везде отличает очередь, активную
  lease и прежний результат.
- После: transport сохраняет валидный JSON независимо от событий; reviewer
  пропускается только для полного fail-closed; origin результата измерим и
  fallback не считается полноценным JTBD-успехом; свежий customer round
  приоритетен; sensitive reads требуют capability/admin либо возвращают только
  synthetic summary; UI честно показывает recovery и историю.
- Не меняется на этом историческом этапе: schemaVersion 2, 700-секундный
  deadline и существовавший тогда Pro/max default,
  MiMo vision, snapshot ground truth, frozen holdout digest, lease/fencing,
  shortage-сценарий как главный, ПРОТЕК только как legacy regression и
  отсутствие реальных ERP/email side effects.

### Владельцы и workflows

- Bridge/web transport: `scripts/agent-bridge.mjs`; Agent contract + Bridge lease.
- Guard/result origin: `lib/agent-guard.mjs`; Agent contract.
- Order/privacy/upload: API routes и `lib/upload-guard.mjs`; API boundary +
  Order lifecycle.
- Recovery presentation: `app/order-stand.tsx` и `lib/client-request.mjs`;
  Observable event.
- Live gate: `tests/run-agent-wow-live.mjs`; agent-rule evaluation.

### Новые обязательные проверки

| Инвариант | Regression |
|---|---|
| Telemetry не уничтожает model result | publication failure + complete partial JSON |
| Fail-closed не тратит reviewer budget | boundary skips; estimate/shortage review remains |
| Безопасный fragment не стирается supplier lead | mixed safe/unsafe authored reply |
| Fresh customer correction wins | `2000 кг` затем `20 кг` |
| Public reads не раскрывают письмо/фото | anonymous detail/ledger/upload negative tests |
| Supplier approval сохраняет и rechecks v2 plan | unchanged/stale snapshot approval tests |
| Generic fallback не получает soft JTBD credit | origin-aware scorer regression |
| Каждый repeat проходит отдельно | one failed repeat makes aggregate NO-GO |
| Старый order не показывается как новый | failed order switch clears active view |

### Rollback

- Откатывать только файл владельца и его тесты при нарушении safety либо
  canonical scenario; не откатывать AgentResult v2, migrations, snapshots,
  lease или frozen fixtures.
- Новые зависимости, скрытый recorded fallback, scenario regex, timeout выше
  700 секунд и production/deploy действия запрещены.

## Переключение primary на официальный DeepSeek API 2026-08-01

### Наблюдаемое поведение

- До: Flash был только экспериментальным selectable alias
  `opencode-go/deepseek-v4-flash`; default и reviewer использовали
  `opencode-go/deepseek-v4-pro/max`.
- После: default primary — `deepseek/deepseek-v4-flash`, официальный endpoint
  `https://api.deepseek.com/v1` и `DEEPSEEK_API_KEY`; OpenCode остаётся
  локальным tool-runner для профиля и `public_webfetch`. Reviewer остаётся
  отдельным `opencode-go/deepseek-v4-pro/max`, vision — MiMo без variant.
- Не меняется: `AgentResult` v2, guard, snapshots, маршрутизация, lease/fencing,
  общий deadline 700 секунд и запрет скрытого recorded fallback.

### Канонический владелец и поверхности

- Provider transport: проектный `opencode.json`; секрет хранится только в
  `.env.local` и передаётся только subprocess direct DeepSeek primary.
- Model selection: `data/models.json` и `scripts/agent-bridge.mjs`.
- Presentation: selector, order event/history/ledger уже читают общий catalog
  и фактический `agentModel`; operator docs описывают direct primary отдельно
  от Go reviewer.
- Compatibility: старый requested Go Flash больше не является default и при
  обработке безопасно нормализуется к текущему catalog default.

### Инварианты и проверки

| Инвариант | Проверка |
|---|---|
| Direct API, а не OpenCode Go alias | provider catalog + auth/tool-call smoke + exact model id |
| Ключ не уходит reviewer/vision | model-scoped `childEnvironment(model)` regression |
| Reviewer остаётся независимым Pro/max | model-role-honesty + bridge spawn assertions |
| Web policy и guard не меняются | agent contract/vision-bridge/full gate |
| UI/ledger показывают фактическую модель | catalog/rendered/ledger tests |
| Версия `0731` не выдумывается | docs/audit source scan; только rolling API id |

### Rollback

- Условие: direct Flash не поддерживает tool loop/JSON contract либо даёт
  safety regression на frozen corpus.
- Действие: вернуть catalog default и локальную env-настройку на Pro/max;
  reviewer, persisted results и migrations не менять.
- Deploy выполнен: нет.

### Итоговая локальная приёмка

- Официальный catalog/auth/tool-call и OpenCode runner smoke прошли; direct
  Flash/max вернул ожидаемый JSON за 5 секунд.
- Targeted model/bridge/ledger: 68/68; privacy/holdout/live-runner: 49/49.
- Полный test gate: 395/395; lint, typecheck, build и `git diff --check` прошли.
- Change contract: PASS; eval readiness: 24/24; contract audit: PASS без
  findings; локальный shortage scenario: PASS с итогом 718 400 ₽.

## Synthesis timeout после live transport truncation 2026-08-03

- До: единственный no-tools synthesis имел cap 90 секунд и не успел завершить
  JSON после раннего обрыва Flash/max.
- После: cap 300 секунд; общий deadline остаётся 700 секунд и программно
  сокращает фактический budget до остатка цепочки.
- Не меняется: один retry, direct DeepSeek Flash/max, Pro/max reviewer,
  fail-closed guard и запрет recorded fallback.
- Regression: production/live constants, shared-deadline allocation, полный
  deterministic gate и повтор исходного live-case.
- Независимая Hermes OAuth Luna/max Role 7: PASS, P0/P1 нет; модель и provider
  подтверждены usage-report как `openai-codex/gpt-5.6-luna`, completed=true.
- Operational webinar status остаётся NO-GO до remote preflight и сохранённого
  live manifest 4 families × 3 repeats; recorded fixtures этот gate не заменяют.

## Live repair: hash-stable bounded web excerpt 2026-08-02

### Наблюдаемое поведение

- До: direct `public_webfetch` успешно открывал страницу и primary создавал
  публичный supplier lead, но при обрыве нормализованного текста ровно на
  пробеле producer хэшировал 12 000 символов с terminal whitespace, а consumer
  повторно делал `trim()` и получал другой SHA-256. Guard корректно удалял
  неподтверждённый lead, поэтому shortage live-gate падал при правильном плане.
- После: producer завершает truncation повторным `trim()` и хэширует ровно тот
  bounded excerpt, который сохраняет consumer. URL-only, snippet и failed page
  по-прежнему не являются evidence; guard thresholds не ослаблены.
- Не меняется: поиск остаётся LLM-first, домены и поставщики не хардкодятся,
  публичные stock/price/delivery требуют подтверждения snapshot/API.

### Инвариант и доказательства

| Инвариант | Regression / evidence |
|---|---|
| Producer SHA равен SHA сохранённого excerpt после допустимой нормализации | synthetic строка длиннее 12 000 с пробелом на границе |
| Excerpt bounded и не заканчивается whitespace | `tests/public-web-fetch.test.mjs` |
| Safety transport и model roles не меняются | public-web, model-role и bridge targeted suites |
| Исправление не привязано к live-домену | production diff не содержит supplier/domain/case branch |

### Итог текущего этапа

- Failure fingerprint: targeted live `wow-08-two-ton-shortage`, callId
  `8d499c95-e86b-408e-83dd-12c8542801fc`, 258 061 ms; business result и
  quality прошли, `supplierLeads=0` из-за hash mismatch.
- Root cause воспроизведён без модели; после фикса safe transport probe дал
  одинаковую длину 11 999 и `hashSame=true`.
- Luna Max usage: `gpt-5.6-luna`, provider `openai-codex`, completed; global
  Hermes reasoning effort — `max`.
- Deploy выполнен: нет.

## Live repair: candidate-bound humane blocker 2026-08-02

### Наблюдаемое поведение

- До: первый certification repeat корректно определил неоднозначную цель и
  задал один вопрос, но generic authored lead вытеснил candidate-specific
  canonical lead. Hard invariants и JTBD прошли, а behavioral quality составил
  62,5%: rapport 1/2, next best action 0/2.
- После: для единственного blocker `target_ambiguity` guard всегда использует
  один canonical lead, собранный из уже проверенных privacy-safe choices:
  отражает конкретные варианты, берёт продолжение на агента и затем задаёт
  ровно один вопрос. Product, price и расчёт в blocker-ответ не попадают.
- Не меняется: смысл цели и choices определяет LLM/vision; guard не содержит
  сценарных объектов, словарей фильмов или case branches; scorer и порог 90%
  не ослаблены.

### Доказательства и экономия live-вызовов

- Failure fingerprint: certification callId
  `e649a67e-b1f4-47ad-a274-a516717645fb`, 319 886 ms, hard 100%, JTBD 100%,
  quality 62,5%.
- Серия остановлена после первого провала: 12/12 уже были невозможны, поэтому
  11 лишних платных вызовов не запускались.
- Regression использует новые произвольные labels `панель`/`дверца`; итоговый
  guard result получает rapport 2/2, next best action 2/2 и quality >=90 при
  неизменном scorer.
- Luna Max usage: `gpt-5.6-luna`, `openai-codex`, completed; Hermes effort
  `max`. Sol удалил только оставшиеся неиспользуемые параметры helper.
- Deploy выполнен: нет.

## Release hardening: direct Flash and synthetic price truth 2026-08-03

### Наблюдаемое поведение

- Primary остаётся direct `deepseek/deepseek-v4-flash`/max через официальный
  DeepSeek API; `opencode-go/deepseek-v4-pro`/max используется только отдельным
  reviewer, MiMo запускается без variant.
- `349 / 361 / 718 400 ₽` явно называются синтетическими учебными данными.
  Публичный web lead не меняет точный расчёт; рабочий оффер требует свежего
  supplier snapshot/API или подтверждённой котировки.
- Frozen holdout manifest снова совпадает с фактическим SHA cases; дублирующий
  explicit-SKU тест удалён без ослабления инициативного SKU regression.
- Активная документация сведена к README, архитектуре, стандарту поведения и
  нейтральному операторскому runbook; завершённый план перенесён в архив.

### Инварианты и проверки

| Инвариант | Проверка |
|---|---|
| Direct Flash не подменён Go provider | model-role-honesty, catalog/provider audit |
| Web price не становится supplier plan | agent guard regressions и UI source copy |
| Учебные цены не выглядят рыночной котировкой | rendered HTML regression |
| Frozen corpus immutable | manifest SHA assertion |
| Документы читают те же owners | contract/eval audits |

### Rollback

- При регрессии откатить только UI/documentation wording и manifest metadata;
  provider routing, AgentResult v2, snapshots, lease и history не менять.
- Deploy выполняется только после полного deterministic и live gate.

## Live repair: deictic target resolution V25 2026-08-02

### Наблюдаемое поведение

- До: raw primary честно сохранял несколько целей на фото, но guard принимал
  общий четырёхсимвольный корень действия или свойства за явное имя цели.
  «Покрась это» могло выбрать candidate с «покрытие», а `Paint this azure` —
  `Azure widget` только по желаемому признаку.
- После: программный override сырой ambiguity распознаёт RU/EN deictic
  reference и требует сильного упоминания identity: один meaningful token для
  однословной цели, минимум два для многословной. Raw primary, уже вернувший
  `resolved`, не переоценивается этим правилом.
- Не меняется: target candidates и visible facts производят MiMo/primary;
  latest customer round остаётся приоритетным; human target не получает
  строительный SKU или estimate; блокировка содержит один вопрос.

### Владелец, инварианты и проверки

- Канонический владелец: `lib/agent-guard.mjs`; consumers — production bridge,
  live runner и сохранённый `AgentResult`; persisted shape и UI не меняются.
- Workflow: Agent contract. Новых dependencies, router, noun parser, словаря
  объектов/цветов, scenario/film branches нет.

| Инвариант | Regression / evidence |
|---|---|
| Action-root не является именем цели | exact V25 replay и neutral action mutant |
| Property-only phrase не выбирает объект | RU `белым`, mixed RU/EN и English `Paint this azure` |
| Явная цель сохраняет инициативность | RU/EN single-token и fully named multiword positives |
| Fresh correction побеждает старую | latest customer correction regression |
| Ambiguity не создаёт коммерческий payload | один blocker, `product=null`, `estimates=[]`, `commitment=none` |
| Human-visible target остаётся безопасным | resolved target без строительного товара/расчёта |

### Rollback и текущее доказательство

- Условие rollback: явная однословная цель перестаёт разрешаться либо
  deictic property снова создаёт auto-choice.
- Действие: откатить только bilingual deictic/strong-mention helper и V25
  regressions; `AgentResult` v2, vision specialization и reviewer contract не
  затрагивать.
- Exact V25 replay после repair: `yellow/needs_info`, четыре grounded choices,
  один вопрос, `product=null`, `estimates=[]`.
- Targeted guard/contract/bridge gate после последней правки: 295/295.
- Остаточный fail-safe: частично названная многословная цель может потребовать
  один вопрос; это безопаснее ложного auto-choice.
- Deploy выполнен: нет.

## Live repair: multi-source SupplierLead completeness V27 2026-08-02

### Наблюдаемое поведение

- До: при нескольких успешно открытых страницах guard сохранял один
  `SupplierLead` на вторую страницу, но добавлял audit-only evidence только для
  первой. Бизнес-план оставался правильным, однако `completeV2Contract`
  закономерно отклонял собственный результат из-за отсутствия exact URL
  evidence для lead.
- После: каждый surviving lead получает ровно одно недостающее нейтральное
  evidence для того же comparable URL с уже проверенными bounded excerpt и
  SHA-256. Существующее exact authored evidence переиспользуется; дубликат не
  создаётся.
- Не меняется: нейтральный artifact имеет `confidence=0`, не становится
  claim evidence и не подтверждает stock/price/delivery; wrong SHA,
  unopened и snippet-only страницы по-прежнему отбрасываются.

### Failure fingerprint и доказательства

- V27 callId `cf225f31-8e00-4949-bc9c-07d1542de778`: shortage business
  invariants 2 000/300/1 700, `ПромКолор Опт`, 718 400 ₽ и manager options
  были корректны; единственный failure — `complete-contract`.
- Открыты две verified страницы; lead ссылался на вторую, evidence содержал
  только первую. Серия остановлена после 7-го artifact, оставшиеся пять
  платных вызовов не запускались.
- Exact raw replay после repair и reviewer approval: matching evidence для
  обеих страниц, `isCompleteAgentResult=true`, supplier plan без изменений.
- Regression покрывает second-page lead, multiple leads, authored-evidence
  dedupe, wrong SHA, unopened и snippet-only источники.

### Границы и rollback

- Канонический владелец: `lib/agent-guard.mjs`; workflow: Agent contract.
- Новых dependencies, supplier/domain/case branches или ослабления
  completeness нет; materialization выполняется только для уже surviving
  normalized leads.
- Rollback: удалить URL-параметр neutral-evidence helper и bounded loop для
  leads, если audit-only evidence начнёт влиять на target/assumptions.
- Deploy выполнен: нет.

## Webinar closeout: four live behavioral invariants 2026-08-03

### Наблюдаемое поведение

- До: свежий Direct Flash/max challenge дал четыре общих дефекта: guard расширил
  корректную пару ambiguous targets посторонними vision candidates; safety
  boundary потерял конкретную ближайшую альтернативу; физически невозможный
  запрос не сохранил прямую формулировку отрицания; неизвестный артикул остался
  в target/assumptions как будто его свойства и фасовка допустимы к оценке.
- После: guard сохраняет уже grounded ambiguity модели; boundary оставляет
  проверяемый соседний ход; неподтверждённые эффекты получают прямую границу;
  неизвестный SKU остаётся только пользовательским фактом и не заражает target,
  assumptions, product или estimates.
- Не меняется: AgentResult V2, БД, API, UI, evaluator thresholds, supplier
  arithmetic и роли моделей. Primary — direct DeepSeek Flash/max, reviewer —
  отдельный Pro/max, vision — MiMo без variant.

### Канонический владелец и consumers

- Источник истины: `lib/agent-guard.mjs` и `public/prompts/sales-agent.md`.
- Consumers: production bridge, live certification runner, order result/history,
  UI и contract/eval audits.
- Workflow: Agent contract. Persistence и публичные shapes не меняются.

### Инварианты и проверки

| Инвариант | Regression |
|---|---|
| Grounded pair не раздувается distracting vision labels | arbitrary-label metamorphic ambiguity case |
| Boundary называет конкретный безопасный scope/verification dimension | positive и unsupported-claim negative mutants |
| Невозможный effect прямо отделён от возможностей покрытия | natural direct-denial positive и magical-claim negative |
| Unknown SKU не создаёт свойства, фасовку или совместимость | arbitrary unknown-code/metamorphic assumptions case |

### Rollback

- Условие: explicit target, valid safety verification или known catalog SKU
  перестают проходить существующий контракт.
- Действие: откатить только текущий guard/prompt delta и четыре regression tests;
  schema, persistence, lease и model routing не затрагивать.
- Deploy выполняется только после targeted live, 10/10, 12/12 и полного gate.

## Webinar closeout: fresh live mutants 2026-08-03

### Наблюдаемое поведение

- До: новый Direct Flash/max run сохранил общий human candidate, хотя его
  собственное vision-evidence и visible fact называли конкретную видимую
  поверхность; evaluator также принял отражение невозможной потребности за
  обещание. Независимый replay показал, что unknown designation мог остаться в
  `SupplierLead.confirmationNeeded`.
- После: generic human candidate уточняется только одной конкретной
  privacy-safe поверхностью, которая одновременно подтверждена его structured
  evidence и отдельным visible fact; конструкция цели с `чтобы` не считается
  обещанием, но прямое положительное утверждение по-прежнему блокируется;
  неизвестный артикул полностью удаляет производные supplier leads.
- Не меняется: raw customer quote, immutable vision observation, public web
  evidence policy, AgentResult V2, model roles, catalog и persistence.

### Инварианты и доказательства

| Инвариант | Regression / evidence |
|---|---|
| Общий `человек` не заменяется выдуманной частью тела | специализация требует unique roots в candidate evidence и visible fact |
| Вопрос остаётся один и без коммерческого payload | exact live wow-01 raw replay после reviewer approval |
| Отражённая потребность не является product promise | `чтобы`-mutant проходит, прямое `покрытие усиливает` падает |
| Unknown SKU не остаётся в supplier-derived structure | opened-lead mutant с designation в `confirmationNeeded` |

### Rollback

- Условие: generic human branch начинает специализироваться при tie/без exact
  support, прямое magic promise проходит или известный SKU теряет supplier plan.
- Действие: откатить только support-based human refinement, intent-clause
  evaluator boundary и unknown-SKU lead reset; остальные live repairs не менять.
- Exact replay текущих wow-01/wow-04 raw artifacts: hard invariants и JTBD 100%.
- Targeted guard/live-runner suites: 251/251.
- Deploy выполнен: нет.
